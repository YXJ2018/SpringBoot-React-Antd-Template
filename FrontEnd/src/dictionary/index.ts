import { default as userStatus } from './userStatus';
import { default as choose } from './choose';
import { default as gender } from './gender';

const dictionary = {
  /**
   * 用户状态字典
   */
  userStatus,
  /**
   * YES OR NO
   */
  choose,
  /**
   * 性别
   */
  gender,
};

export default dictionary;
